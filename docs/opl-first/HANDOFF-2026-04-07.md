# Handoff de desarrollo — 2026-04-07

## Situación

El repositorio tiene **cambios sin commit y 12 commits sin push**. Antes de cualquier trabajo:

```bash
# 1. Ver qué hay sin commit
git status
git diff --stat

# 2. Commit de la remediación categorial
git add \
  CLAUDE.md \
  docs/opl-first/README.md \
  docs/opl-first/10-isomorphism-architecture.md \
  packages/core/src/semantic-kernel.ts \
  packages/core/src/opl-compile.ts \
  packages/core/src/opl.ts \
  packages/core/src/index.ts \
  packages/core/tests/compile-to-kernel.test.ts \
  packages/core/tests/isomorphism-laws.test.ts \
  packages/web/src/lib/projection-view.ts \
  packages/web/tests/opd-canvas-projection.test.tsx \
  packages/web/vitest.config.ts \
  vitest.config.ts

git rm packages/web/tests/opd-canvas-projection.test.ts

git commit -m "feat(core): ADR-003 isomorphism remediation — compileToKernel, kernel-native render, 4-law tests"

# 3. Push TODO (12 commits previos + nuevo)
git push
```

Hay también cambios en `methodology.ts`, `api.ts`, `docs/opm-methodology.md` que parecen de una sesión previa — commitear por separado.

## Qué cambió en esta sesión

### Cambios arquitectónicos (ADR-003 vinculante)

| Cambio | Archivo | Impacto |
|--------|---------|---------|
| SemanticKernel completado | `semantic-kernel.ts` | Nuevos types `SemanticModifier`, `SemanticFan`; nuevos maps `modifiers`, `fans` en `SemanticKernel`; adapters `semanticKernelFromModel` y `legacyModelFromSemanticKernel` preservan mod/fan |
| `compileToKernel()` | `opl-compile.ts` | Nuevo functor F1 (Parse: 𝒫 → 𝒮). Compila OPL → SemanticKernel preservando `sourceInfo`, `InZoomStep[]`, y `origin: "derived-in-zoom"` |
| `renderAllFromKernelNative()` | `opl.ts` | Nuevo functor F2 (Render: 𝒮 → 𝒫). Render OPL desde kernel+atlas sin pasar por Model |
| Isomorphism ADR | `docs/opl-first/10-isomorphism-architecture.md` | Documento vinculante con 4 leyes |
| CLAUDE.md binding | `CLAUDE.md` | Sección nueva que prohíbe layout→semantics coupling |

### Fixes

| Fix | Archivo |
|-----|---------|
| `hiddenStateCount` ahora incluye suppressed states | `projection-view.ts:152` |
| Test JSX renombrado `.ts` → `.tsx` | `opd-canvas-projection.test.tsx` |
| Vitest configs aceptan `.tsx` | `vitest.config.ts` (root + web) |

### Tests nuevos

| Archivo | Tests | Qué verifican |
|---------|-------|---------------|
| `compile-to-kernel.test.ts` | 11 | sourceInfo, modifiers, fans, InZoomStep[], derived invocations, 6 fixtures |
| `isomorphism-laws.test.ts` | 15 | 4 leyes × 6 fixtures (con tolerancias para gaps de roundtrip conocidos) |

## Estado de los tests

```bash
bun run test        # 1092 tests, 0 fail, 5 todo
bun run typecheck:core  # green
bun run web:build       # green
```

## Qué NO cambió

- `simulation.ts` — sigue leyendo `model.appearances` en 6 sitios
- `expose()` en `opl.ts` — la función legacy sigue intacta
- `compileOplDocuments()` — intacta, `compileToKernel` la envuelve
- Todos los 1066 tests previos siguen pasando

## Cómo continuar

### Opción A: Seguir con la remediación (Fase 5)

Desacoplar `simulation.ts` de `model.appearances`. Los 6 sitios:
- `getExecutableProcesses` (líneas 125-135): usa `app.y` para ordenar subprocesos
- `resolveLinksForOpd` (líneas 227-363): 5 reads de appearances para visibilidad

Approach: crear versiones `*FromKernel` que usen `kernel.refinements.steps` para ordering y `atlas.nodes[opdId].visibleThings` para visibilidad. Mantener las funciones legacy intactas.

### Opción B: Cerrar roundtrip gaps

Los 5 `it.todo` en `categorical-lens.test.ts` (líneas 194-200) documentan roundtrips rotos en fixtures con nombres compuestos en español. Las leyes del isomorfismo usan tolerancias (~5% delta) por estos gaps.

### Opción C: Cerrar Fases 4-5 del OPL-first

- Source mapping bidireccional (click OPL ↔ visual). Ahora factible porque `compileToKernel` preserva `sourceInfo` con spans.
- Editor OPL como superficie principal (promover `OplEditorView` de tab a panel principal).

## Nuevo pipeline disponible

```
OPL text
  → parseOplDocument(s)     # existente
  → compileToKernel(docs)   # NUEVO — preserva sourceInfo, InZoomStep[], mod/fan
  → SemanticKernel           # COMPLETADO — functor fiel con 11 maps
  → exposeSemanticKernel()   # existente
  → OpdAtlas                 # existente
  → renderAllFromKernelNative(kernel, atlas)  # NUEVO — sin Model roundtrip
  → OPL text                # roundtrip verificado por 4 leyes
```

Para consumidores web que necesitan `Model`:
```
kernel → legacyModelFromSemanticKernel(kernel, atlas, layout) → Model  # ahora preserva mod/fan
```

## Reglas vinculantes (de CLAUDE.md)

- No introducir dependencia layout→semántica en funciones nuevas
- No hacer round-trip por Model en funciones kernel-native
- Usar `compileToKernel` (no `compileOplDocuments`) para pipeline OPL-first nuevo
- Las 4 leyes del isomorfismo deben pasar después de cualquier cambio al pipeline
