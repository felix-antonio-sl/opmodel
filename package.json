{
  "name": "opmodel",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bunx --bun vitest run",
    "test:watch": "bunx --bun vitest"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "vitest": "^4.0.18"
  }
}
